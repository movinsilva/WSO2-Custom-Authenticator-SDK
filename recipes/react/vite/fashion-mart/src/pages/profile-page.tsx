import Layout from "../layouts/layout";
import { Profile } from "@asgardeo/react-ui";

const ProfilePage: React.FC = () => {
  return (
    <Layout>
      <div>
        <Profile />
      </div>
    </Layout>
  );
};

export default ProfilePage;
